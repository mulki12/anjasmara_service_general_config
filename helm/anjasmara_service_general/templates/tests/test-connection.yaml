apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anjasmara-service-general.fullname" . }}-test-connection"
  labels:
{{ include "anjasmara-service-general.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "anjasmara-service-general.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
